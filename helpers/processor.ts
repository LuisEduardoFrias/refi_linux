import codi from 'codi';

export default function processor(code : string ) {
 code.
}

function validarPalabraEnPrimerIndice(text: string) {

  const later = cadena.split(' ');

  if (palabras.length > 0 && palabras[0] === palabra) {
   
  }
}

function execite() {
 switch(code) {
  case 'cd' {
   cd(ruta);
  },
  case "mkdir": {
   mkdir(folderName)
  },
  case "touch": {
   touch(fileName)
  },
  case "ls": {
   ls()
  },
  case "pwd": {
   pwd()
  }
  case "echo": {
   echo()
  }
  case "cat": {
   cat()
  },
  case "rm": {
   rm()
  },
  case "cp": {
   cp()
  },
  case "move": {
   move()
  }
 }
}